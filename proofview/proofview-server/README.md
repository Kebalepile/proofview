# ProofView Server (Vanilla Node.js)

A dependency-free Node.js server for ProofView tracking:
- Email open tracking via image requests
- Link click tracking via redirect
- Document download tracking via streamed attachment
- Events feed for Chrome extension polling (notifications)
- Message counters/status

## Requirements
- Node.js 18+ recommended

## Run
```bash
node src/server.js
